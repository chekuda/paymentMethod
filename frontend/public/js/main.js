   /**
      *Main Controller
      @vepromptctr: vePrompt tool controller module
      @insertImagestoDB: Insert images in DB (administrator)
      @vecontactCtr: veContact tool controller module
    **/

    var app = angular.module('paymentMethod', ['paymentCtr']);

